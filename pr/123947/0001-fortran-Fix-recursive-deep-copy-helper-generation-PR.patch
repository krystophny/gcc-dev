From 841b68e48c448c4d5e60ee9926904290fd7e66ef Mon Sep 17 00:00:00 2001
From: Christopher Albert <albert@tugraz.at>
Date: Sun, 22 Feb 2026 22:23:51 +0100
Subject: [PATCH] fortran: Fix recursive deep-copy helper generation [PR123947]

Fix recursive allocatable deep-copy expansion in structure allocation
and make copy-helper wrapper reuse safe across function contexts by
caching FUNCTION_DECLs and rebuilding ADDR_EXPR at use sites.

Keep recursion handling finite for mutually recursive derived types
and add regression coverage for both original and SOURCE-allocation
variants.

	PR fortran/123947

gcc/fortran/ChangeLog:

	* trans-array.cc (copy_helper_wrappers): New.  Cache wrapper
	FUNCTION_DECLs by derived type.
	(generate_element_copy_wrapper): Reuse cached wrapper decl and
	rebuild ADDR_EXPR per use site; register wrapper before recursive
	body generation.
	(structure_alloc_comps): Use recursive helper path for direct
	self-recursion only; remove generating_copy_helper flag.

gcc/testsuite/ChangeLog:

	* gfortran.dg/pr123947.f90: New test.
	* gfortran.dg/pr123947_2.f90: New test.

Signed-off-by: Christopher Albert <albert@tugraz.at>
---
 gcc/fortran/trans-array.cc               | 52 ++++++++++++------------
 gcc/testsuite/gfortran.dg/pr123947.f90   | 29 +++++++++++++
 gcc/testsuite/gfortran.dg/pr123947_2.f90 | 47 +++++++++++++++++++++
 3 files changed, 102 insertions(+), 26 deletions(-)
 create mode 100644 gcc/testsuite/gfortran.dg/pr123947.f90
 create mode 100644 gcc/testsuite/gfortran.dg/pr123947_2.f90

diff --git a/gcc/fortran/trans-array.cc b/gcc/fortran/trans-array.cc
index 6cddd80b8ae..15b20a6ec06 100644
--- a/gcc/fortran/trans-array.cc
+++ b/gcc/fortran/trans-array.cc
@@ -10033,8 +10033,8 @@ enum {DEALLOCATE_ALLOC_COMP = 1, NULLIFY_ALLOC_COMP,
       BCAST_ALLOC_COMP};
 
 static gfc_actual_arglist *pdt_param_list;
-static bool generating_copy_helper;
 static hash_set<gfc_symbol *> seen_derived_types;
+static hash_map<gfc_symbol *, tree> copy_helper_wrappers;
 
 /* Forward declaration of structure_alloc_comps for wrapper generator.  */
 static tree structure_alloc_comps (gfc_symbol *, tree, tree, int, int, int,
@@ -10043,7 +10043,11 @@ static tree structure_alloc_comps (gfc_symbol *, tree, tree, int, int, int,
 /* Generate a wrapper function that performs element-wise deep copy for
    recursive allocatable array components. This wrapper is passed as a
    function pointer to the runtime helper _gfortran_cfi_deep_copy_array,
-   allowing recursion to happen at runtime instead of compile time.  */
+   allowing recursion to happen at runtime instead of compile time.
+
+   Wrappers are cached by derived type.  The cache stores the wrapper
+   FUNCTION_DECL and each use-site rebuilds a fresh ADDR_EXPR to avoid
+   reusing context-sensitive expression trees across functions.  */
 
 static tree
 get_copy_helper_function_type (void)
@@ -10070,6 +10074,10 @@ static tree
 generate_element_copy_wrapper (gfc_symbol *der_type, tree comp_type,
 				int purpose, int caf_mode)
 {
+  tree *cached = copy_helper_wrappers.get (der_type);
+  if (cached)
+    return build1 (ADDR_EXPR, get_copy_helper_pointer_type (), *cached);
+
   tree fndecl, fntype, result_decl;
   tree dest_parm, src_parm, dest_typed, src_typed;
   tree der_type_ptr;
@@ -10093,6 +10101,7 @@ generate_element_copy_wrapper (gfc_symbol *der_type, tree comp_type,
   DECL_CONTEXT (fndecl) = NULL_TREE;
   DECL_INITIAL (fndecl) = make_node (BLOCK);
   BLOCK_SUPERCONTEXT (DECL_INITIAL (fndecl)) = fndecl;
+  copy_helper_wrappers.put (der_type, fndecl);
 
   result_decl = build_decl (input_location, RESULT_DECL, NULL_TREE,
 			    void_type_node);
@@ -10122,9 +10131,6 @@ generate_element_copy_wrapper (gfc_symbol *der_type, tree comp_type,
   pushlevel ();
   gfc_init_block (&block);
 
-  bool saved_generating = generating_copy_helper;
-  generating_copy_helper = true;
-
   /* When generating a wrapper, we need a fresh type tracking state to
      avoid inheriting the parent context's seen_derived_types, which would
      cause infinite recursion when the wrapper tries to handle the same
@@ -10152,7 +10158,6 @@ generate_element_copy_wrapper (gfc_symbol *der_type, tree comp_type,
   for (unsigned i = 0; i < saved_symbols.length (); i++)
     seen_derived_types.add (saved_symbols[i]);
   saved_symbols.release ();
-  generating_copy_helper = saved_generating;
 
   body = gfc_finish_block (&block);
   decls = getdecls ();
@@ -10332,8 +10337,8 @@ structure_alloc_comps (gfc_symbol * der_type, tree decl, tree dest,
 	   && seen_derived_types.contains (c->ts.u.derived))
 	  || (c->ts.type == BT_CLASS
 	      && seen_derived_types.contains (CLASS_DATA (c)->ts.u.derived));
-      bool inside_wrapper = generating_copy_helper;
-
+      bool direct_self_recursive
+	= (c->ts.type == BT_DERIVED && c->ts.u.derived == der_type);
       bool is_pdt_type = IS_PDT (c);
 
       cdecl = c->backend_decl;
@@ -11007,14 +11012,15 @@ structure_alloc_comps (gfc_symbol * der_type, tree decl, tree dest,
 					   false, false, NULL_TREE, NULL_TREE);
 	      gfc_add_expr_to_block (&fnblock, tmp);
 	    }
-      /* Special case: recursive allocatable array components require
-	 runtime helpers to avoid compile-time infinite recursion.  Generate
-	 a call to _gfortran_cfi_deep_copy_array with an element copy
-	 wrapper.  When inside a wrapper, reuse current_function_decl.  */
-      else if (c->attr.allocatable && c->as && cmp_has_alloc_comps && same_type
-	       && purpose == COPY_ALLOC_COMP && !c->attr.proc_pointer
-	       && !c->attr.codimension && !caf_in_coarray (caf_mode)
-	       && c->ts.type == BT_DERIVED && c->ts.u.derived != NULL)
+	  /* Special case: recursive allocatable array components require
+	     runtime helpers to avoid compile-time infinite recursion.  Generate
+	     a call to _gfortran_cfi_deep_copy_array with an element copy
+	     wrapper.  */
+	  else if (c->attr.allocatable && c->as && cmp_has_alloc_comps
+		   && direct_self_recursive
+		   && purpose == COPY_ALLOC_COMP && !c->attr.proc_pointer
+		   && !c->attr.codimension && !caf_in_coarray (caf_mode)
+		   && c->ts.type == BT_DERIVED && c->ts.u.derived != NULL)
 	    {
 	      tree copy_wrapper, call, dest_addr, src_addr, elem_type;
 	      tree helper_ptr_type;
@@ -11037,16 +11043,10 @@ structure_alloc_comps (gfc_symbol * der_type, tree decl, tree dest,
 							     comp_rank);
 	      gfc_add_expr_to_block (&fnblock, alloc_expr);
 
-	      /* Generate or reuse the element copy helper.  Inside an
-		 existing helper we can reuse the current function to
-		 prevent recursive generation.  */
-	      if (inside_wrapper)
-		copy_wrapper
-		  = gfc_build_addr_expr (NULL_TREE, current_function_decl);
-	      else
-		copy_wrapper
-		  = generate_element_copy_wrapper (c->ts.u.derived, elem_type,
-						   purpose, caf_mode);
+	      /* Generate or reuse the element copy helper.  */
+	      copy_wrapper
+		= generate_element_copy_wrapper (c->ts.u.derived, elem_type,
+						 purpose, caf_mode);
 	      copy_wrapper = fold_convert (helper_ptr_type, copy_wrapper);
 
 	      /* Build addresses of descriptors.  */
diff --git a/gcc/testsuite/gfortran.dg/pr123947.f90 b/gcc/testsuite/gfortran.dg/pr123947.f90
new file mode 100644
index 00000000000..30a21864f65
--- /dev/null
+++ b/gcc/testsuite/gfortran.dg/pr123947.f90
@@ -0,0 +1,29 @@
+! { dg-do compile }
+!
+! PR fortran/123947
+! Reduced from Bugzilla attachment 63564.
+! Used to ICE in gfc_build_addr_expr from trans-array deep-copy handling.
+!
+TYPE TY0
+TYPE(TY0), ALLOCATABLE :: NODE0(:)
+TYPE(TY), ALLOCATABLE :: NODE4(:)
+TYPE(TY1),ALLOCATABLE :: NODE7(:)
+END TYPE
+TYPE TY1
+INTEGER,ALLOCATABLE :: II(:)
+TYPE(TY0) :: NODE2(4)
+TYPE(TY1), ALLOCATABLE :: NODE1(:)
+TYPE(TY),ALLOCATABLE :: NODE5(:)
+END TYPE
+TYPE TY
+INTEGER,ALLOCATABLE :: II(:)
+TYPE(TY1) :: NODE3(4)
+TYPE(TY),ALLOCATABLE :: NODE6(:)
+END TYPE
+TYPE BASE
+TYPE(TY),ALLOCATABLE :: OBJ(:)
+END TYPE
+TYPE(BASE):: OBJ_BASE
+ALLOCATE(OBJ_BASE%OBJ(2)%NODE6(2)%NODE6(3))
+ALLOCATE(OBJ_BASE%OBJ(2)%NODE6(2)%NODE6(2)%NODE6(3))
+END
diff --git a/gcc/testsuite/gfortran.dg/pr123947_2.f90 b/gcc/testsuite/gfortran.dg/pr123947_2.f90
new file mode 100644
index 00000000000..d6c26c48b06
--- /dev/null
+++ b/gcc/testsuite/gfortran.dg/pr123947_2.f90
@@ -0,0 +1,47 @@
+! { dg-do compile }
+! { dg-options "-O2" }
+!
+! PR fortran/123947
+! Keep recursive allocatable array deep-copy helper generation valid when
+! SOURCE allocation is expanded in more than one procedure.
+
+module pr123947_mod
+  type :: node_t
+    integer, allocatable :: value
+    type(node_t), allocatable :: next(:)
+  end type node_t
+
+  type :: holder_t
+    type(node_t), allocatable :: src(:)
+    type(node_t), allocatable :: dst(:)
+  end type holder_t
+contains
+  subroutine seed_graph(a)
+    type(node_t), allocatable, intent(out) :: a(:)
+    allocate (a(3))
+    allocate (a(1)%value)
+    allocate (a(2)%value)
+    allocate (a(2)%next(1))
+    allocate (a(2)%next(1)%value)
+    allocate (a(3)%next(2))
+    allocate (a(3)%next(2)%next(2))
+  end subroutine seed_graph
+
+  subroutine clone_part_one()
+    type(holder_t) :: h
+    call seed_graph(h%src)
+    allocate (h%dst, source=h%src(1:2))
+  end subroutine clone_part_one
+
+  subroutine clone_part_two()
+    type(holder_t) :: h
+    call seed_graph(h%src)
+    allocate (h%dst, source=h%src(2:3))
+  end subroutine clone_part_two
+end module pr123947_mod
+
+program pr123947_2
+  use pr123947_mod
+  call clone_part_one()
+  call clone_part_two()
+end program pr123947_2
-- 
2.53.0

