From 309b44473d460ddffff5629b1e8240091c0f51d7 Mon Sep 17 00:00:00 2001
From: Christopher Albert <albert@tugraz.at>
Date: Fri, 7 Nov 2025 19:41:57 +0100
Subject: [PATCH] fortran: Replace hash_set with C-style linked list in
 trans-array.cc

Replace the C++ hash_set<gfc_symbol *> with a simple C-style linked list
for tracking seen derived types during recursive type processing.

This change:
- Removes C++ hash_set dependency
- Adds struct seen_type_node for linked list nodes
- Adds seen_type_list_contains() helper function
- Replaces .add(), .contains(), and .remove() with C operations

gcc/fortran/ChangeLog:

	* trans-array.cc (seen_type_node): New struct for linked list.
	(seen_type_list_contains): New helper function.
	(structure_alloc_comps): Replace hash_set with linked list.
---
 gcc/fortran/trans-array.cc | 38 +++++++++++++++++++++++++++++++++-----
 1 file changed, 33 insertions(+), 5 deletions(-)

diff --git a/gcc/fortran/trans-array.cc b/gcc/fortran/trans-array.cc
index 1bfc0ce46b8..aa7f6b4c91e 100644
--- a/gcc/fortran/trans-array.cc
+++ b/gcc/fortran/trans-array.cc
@@ -10144,6 +10144,23 @@ generate_element_copy_wrapper (gfc_symbol *der_type, tree comp_type,
   return build1 (ADDR_EXPR, get_copy_helper_pointer_type (), fndecl);
 }
 
+/* Linked list for tracking seen derived types.  */
+struct seen_type
+{
+  gfc_symbol *sym;
+  struct seen_type *next;
+};
+
+/* Check if a symbol is already in the seen list.  */
+static bool
+seen_contains (struct seen_type *list, gfc_symbol *sym)
+{
+  for (struct seen_type *p = list; p; p = p->next)
+    if (p->sym == sym)
+      return true;
+  return false;
+}
+
 static tree
 structure_alloc_comps (gfc_symbol * der_type, tree decl, tree dest,
 		       int rank, int purpose, int caf_mode,
@@ -10173,7 +10190,7 @@ structure_alloc_comps (gfc_symbol * der_type, tree decl, tree dest,
   int caf_dereg_mode;
   symbol_attribute *attr;
   bool deallocate_called;
-  static hash_set<gfc_symbol *> seen_derived_types;
+  static struct seen_type *seen_derived_types = NULL;
 
   gfc_init_block (&fnblock);
 
@@ -10296,7 +10313,11 @@ structure_alloc_comps (gfc_symbol * der_type, tree decl, tree dest,
     }
   /* Otherwise, act on the components or recursively call self to
      act on a chain of components.  */
-  seen_derived_types.add (der_type);
+  struct seen_type *node = XNEW (struct seen_type);
+  node->sym = der_type;
+  node->next = seen_derived_types;
+  seen_derived_types = node;
+
   for (c = der_type->components; c; c = c->next)
     {
       bool cmp_has_alloc_comps = (c->ts.type == BT_DERIVED
@@ -10304,9 +10325,10 @@ structure_alloc_comps (gfc_symbol * der_type, tree decl, tree dest,
 				    && c->ts.u.derived->attr.alloc_comp;
       bool same_type
 	= (c->ts.type == BT_DERIVED
-	   && seen_derived_types.contains (c->ts.u.derived))
+	   && seen_contains (seen_derived_types, c->ts.u.derived))
 	  || (c->ts.type == BT_CLASS
-	      && seen_derived_types.contains (CLASS_DATA (c)->ts.u.derived));
+	      && seen_contains (seen_derived_types,
+				CLASS_DATA (c)->ts.u.derived));
       bool inside_wrapper = generating_copy_helper;
 
       bool is_pdt_type = c->ts.type == BT_DERIVED
@@ -11397,7 +11419,13 @@ structure_alloc_comps (gfc_symbol * der_type, tree decl, tree dest,
 	  break;
 	}
     }
-  seen_derived_types.remove (der_type);
+  /* Remove der_type from seen list.  */
+  if (seen_derived_types && seen_derived_types->sym == der_type)
+    {
+      struct seen_type *tmp = seen_derived_types;
+      seen_derived_types = tmp->next;
+      free (tmp);
+    }
 
   return gfc_finish_block (&fnblock);
 }
-- 
2.51.2

